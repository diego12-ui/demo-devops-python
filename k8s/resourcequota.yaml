apiVersion: v1
kind: ResourceQuota
metadata:
  name: devsu-demo-python-quota
spec:
  hard:
    requests.cpu: "300m"
    requests.memory: 384Mi
    limits.cpu: "900m"
    limits.memory: 1Gi
    pods: "5"
    services: "5"
